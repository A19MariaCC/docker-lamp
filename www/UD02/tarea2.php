<?php
    // Indica cuál de los siguientes son nombres de variables válidas e inválidos e indica por qué:
- valor // Variable inválida -> Variable correctamente declarada $valor
- $_N // Variable válida, la variable puede ser declarada con un guión bajo
- $valor_actual // Variable válida, se puede utilizar el guión bajo para separar palabras
- $n // Variable válida, las variables pueden nombrarse con un solo caracter precedido del símbolo $
- $#datos //V ariable inválida, las variables no pueden comenzar por caracteres especiales, en este caso por el símbolo #
- $valorInicial0 // Variable válida, las variables pueden llevar números siempre que no sea al principio
- $proba,valor // La primera variable es válida, está correctamente declarada, la segunda variable debe llevar el símbolo $,
- $2saldo // Variable inválida, no se puede utilizar números al comienzo del nombre de una variable para declararla
- $n // La declaración de la variable es válida, siempre y cuando no queramos almacenar valores diferentes con la misma nomenclatura, ya que no se puede repetir
- $meuProblema // Variable válida, se utiliza el formato CamelCase y no hay espacios en blanco entre ambas palabras
- $meu Problema // Variable inválida, no se pueden dejar espacios en blanco
- $echo // No se debe utilizar la palabra echo para declarar una variable puesto que se trata de una palabra reservada del lenguaje PHP
- $m&m //  Variable inválida, al contener el caracter &, el siguiente caracter lo entiende como una referencia a otra variable
- $registro //Variable válida, correctamente declarada
- $ABC // Variable válida, se distingue mayúsculas de minúsculas por lo que $ABC no es lo mismo que $abc
- $85 Nome // Variable inválida, los nombres de las variables no pueden empezar por números y en este caso además hay un espacio en blanco
- $AAAAAAAAA // Variable válida, aunque se recomienda que las variables hagan referencia a su contenido
- $nome_apelidos // Variable válida, se puede utilizar el guión bajo para separar palabras
- $saldoActual // Variable válida, se utiliza la nomenclatura camelCase
- $92 // Variable inválida puesto que los nombres de las variables no pueden comenzar por números
- $*143idade // Variable inválida puesto que utiliza el caracter * 

?>